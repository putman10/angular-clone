export class Article {
  constructor(public title: string, public subtitle: string, public author: string, public image: string, public bodyText: string, public category: string, public publishedDate: string){};
}
